/**
 * @file
 * Custom JavaScript for Mantle2 theme.
 */

(function (Drupal, drupalSettings) {
  'use strict';

  /**
   * Mantle theme behaviors.
   */
  Drupal.behaviors.mantleTheme = {
    attach: function (context, settings) {
      console.log('Mantle theme initialized');
      
      // Add custom JavaScript functionality here
    }
  };

})(Drupal, drupalSettings);